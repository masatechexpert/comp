.wrap2
  .group
    .group__title
      %h1 グループ一覧
    .group__index
    - current_user.groups.each do |group|
      = link_to group_messages_path(group) do
        .group__index__name
          = group.name